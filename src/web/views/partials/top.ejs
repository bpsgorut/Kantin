<!doctype html>
<html lang="id">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300..700&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body>
    <div class="checkerboard-strip"></div>
    <header class="header">
      <div class="container header__inner">
        <div class="brand">
          <a href="/transactions" class="brand__link">Kantin <span style="color: var(--text)">Ke-</span>JYUJYUR<span style="color: var(--text)">-an</span></a>
        </div>
        <nav class="nav">
          <a class="nav__link <%= active === 'transactions' ? 'is-active' : '' %>" href="/transactions">Transaksi</a>
          <a class="nav__link <%= active === 'products' ? 'is-active' : '' %>" href="/products">Produk</a>
        </nav>
      </div>
    </header>
    <main class="container main">
